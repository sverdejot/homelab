#!/usr/bin/env bash
#MISE description="Build and flash agent SD image. Usage: mise run flash:agent <node-number> <device> (e.g. 1 rdisk4)"
set -euo pipefail
NODE=${1:?node number required}
DEVICE=${2:?device required}
mise run build:agent "$NODE"
mise run flash:sd:agent "$NODE" "$DEVICE"
