#!/usr/bin/env bash
#MISE description="Build and flash master installer SD image. Usage: mise run flash:master <node-number> <device> (e.g. 0 rdisk4)"
set -euo pipefail
NODE=${1:?node number required}
DEVICE=${2:?device required}
mise run build:installer
mise run flash:sd:master "$DEVICE"
