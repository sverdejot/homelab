---
cluster:
  instances: 1
  storage:
    size: 8Gi
  roles:
    - name: maybe
      ensure: present
      comment: Maybe - Financial app
      login: true
      createdb: true
      superuser: false
      passwordSecret:
        name: maybe/maybe-db-auth
  monitoring:
    enabled: true
